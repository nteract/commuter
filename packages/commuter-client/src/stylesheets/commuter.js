import { StyleSheet } from "aphrodite";

const styles = StyleSheet.create({
  outerContainer: { fontFamily: "sans-serif" },
  innerContainer: { paddingTop: "10px" },
  divider: { marginTop: "0rem" },
  navMargin: { marginLeft: "1rem" }
});

export { styles };
